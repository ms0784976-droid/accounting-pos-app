#!/usr/bin/env node
/**
 * generate-files.js
 * ------------------------------------------------------------------
 * يولّد تلقائياً كل ملفات نظام Supabase + المصادقة داخل مشروعك.
 *
 * طريقة الاستخدام:
 *   1) انسخ هذا الملف إلى جذر مشروع Next.js (بجانب package.json)
 *   2) شغّل: node generate-files.js
 *   3) نفّذ: npm install @supabase/supabase-js @supabase/ssr
 *   4) أنشئ ملف .env.local من .env.local.example وامْلأ القيم الحقيقية
 *   5) نفّذ محتوى supabase/schema.sql داخل Supabase SQL Editor
 * ------------------------------------------------------------------
 * ملاحظة: السكربت لن يستبدل أي ملف موجود مسبقاً إلا لو مررت --force
 */

const fs = require("fs");
const path = require("path");

const FORCE = process.argv.includes("--force");

const files = {
  ".env.local.example": "# ============================================================\n# متغيرات Supabase — انسخ هذا الملف باسم .env.local واملأ القيم\n# تجدها في: Supabase Dashboard → Project Settings → API\n# ============================================================\n\n# NEXT_PUBLIC_* تكون مرئية في المتصفح (Client-Side) — هذا مقصود وآمن\n# لأن anon key محمي فعلياً عبر سياسات RLS في قاعدة البيانات\nNEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co\nNEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\n\n# service_role key — سري تماماً، لا تضعه أبداً في كود يعمل بالمتصفح\n# يُستخدم فقط داخل Server Actions / API Routes (على السيرفر حصراً)\n# تجده في نفس الصفحة تحت \"service_role\" (وليس \"anon public\")\nSUPABASE_SERVICE_ROLE_KEY=eyJxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\n",
  "supabase/schema.sql": "-- ============================================================\n-- مُحاسِب — Supabase Schema\n-- نفّذ هذا الملف كاملاً في: Supabase Dashboard → SQL Editor\n-- ============================================================\n\n-- --------------------------------------------------------------\n-- 1. جدول الأدوار (Roles)\n--    كل مستخدم في auth.users له صف واحد هنا يحدد: owner أم client\n-- --------------------------------------------------------------\ncreate table if not exists public.user_roles (\n  user_id uuid primary key references auth.users(id) on delete cascade,\n  role text not null check (role in ('owner', 'client')),\n  created_at timestamptz not null default now()\n);\n\nalter table public.user_roles enable row level security;\n\n-- المستخدم يقدر يقرأ دوره فقط (لا شيء أكثر)\ncreate policy \"users can read own role\"\n  on public.user_roles for select\n  using (auth.uid() = user_id);\n\n-- --------------------------------------------------------------\n-- 2. دالة مساعدة: is_owner()\n--    SECURITY DEFINER لتفادي التكرار اللانهائي في سياسات RLS\n--    عند التحقق من دور المستخدم داخل سياسة تخص جدول آخر\n-- --------------------------------------------------------------\ncreate or replace function public.is_owner()\nreturns boolean\nlanguage sql\nsecurity definer\nset search_path = public\nstable\nas $$\n  select exists (\n    select 1 from public.user_roles\n    where user_id = auth.uid() and role = 'owner'\n  );\n$$;\n\n-- --------------------------------------------------------------\n-- 3. جدول tenants (بيانات النشاط التجاري فقط — بلا كلمة مرور)\n--    كلمة المرور والتحقق منها يديرهما Supabase Auth تلقائياً\n--    عبر auth.users (مرتبط بـ user_id)\n-- --------------------------------------------------------------\ncreate table if not exists public.tenants (\n  id uuid primary key default gen_random_uuid(),\n  user_id uuid not null unique references auth.users(id) on delete cascade,\n  name text not null,              -- اسم النشاط التجاري\n  owner_name text not null,        -- اسم صاحب النشاط\n  email text not null,\n  phone text default '',\n  plan text not null default 'basic' check (plan in ('basic', 'professional', 'enterprise')),\n  status text not null default 'active' check (status in ('active', 'frozen', 'trial')),\n  industry text default '',\n  currency text default 'SAR',\n  expires_at date,\n  created_at timestamptz not null default now()\n);\n\nalter table public.tenants enable row level security;\n\n-- المالك (Owner) يرى ويعدّل كل شيء\ncreate policy \"owner full access on tenants\"\n  on public.tenants for all\n  using (public.is_owner())\n  with check (public.is_owner());\n\n-- كل عميل يرى صف نشاطه التجاري فقط (قراءة فقط، لا يعدّل بيانات اشتراكه بنفسه)\ncreate policy \"tenant can read own row\"\n  on public.tenants for select\n  using (auth.uid() = user_id);\n\n-- --------------------------------------------------------------\n-- 4. Index لتسريع البحث بالإيميل عند الحاجة\n-- --------------------------------------------------------------\ncreate index if not exists tenants_email_idx on public.tenants (email);\n\n-- --------------------------------------------------------------\n-- 5. إنشاء حساب المالك (Owner) — نفّذ مرة واحدة فقط، يدوياً\n-- --------------------------------------------------------------\n-- الخطوة أ: أنشئ المستخدم من Dashboard → Authentication → Users → Add user\n--           (فعّل \"Auto Confirm User\" حتى لا يحتاج تأكيد بريد)\n--           ضع بريده وكلمة مروره، ثم انسخ الـ UID الظاهر بعد الإنشاء\n--\n-- الخطوة ب: نفّذ هذا الاستعلام (استبدل UUID الحقيقي):\n--\n-- insert into public.user_roles (user_id, role)\n-- values ('ضع-uid-المالك-هنا', 'owner');\n",
  "lib/supabase/client.ts": "// عميل Supabase لاستخدامه داخل مكونات \"use client\" في المتصفح.\n// آمن بالكامل لأنه يستخدم anon key فقط (محمي عبر RLS، وليس عبر إخفاء المفتاح).\n\nimport { createBrowserClient } from \"@supabase/ssr\"\nimport type { Database } from \"./types\"\n\nexport function createClient() {\n  return createBrowserClient<Database>(\n    process.env.NEXT_PUBLIC_SUPABASE_URL!,\n    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!\n  )\n}\n",
  "lib/supabase/server.ts": "// عميل Supabase لاستخدامه داخل Server Actions / Server Components.\n// يقرأ ويكتب جلسة المستخدم (Session) عبر Cookies بشكل آمن على السيرفر.\n\nimport { createServerClient } from \"@supabase/ssr\"\nimport { cookies } from \"next/headers\"\nimport type { Database } from \"./types\"\n\nexport async function createClient() {\n  const cookieStore = await cookies()\n\n  return createServerClient<Database>(\n    process.env.NEXT_PUBLIC_SUPABASE_URL!,\n    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,\n    {\n      cookies: {\n        getAll() {\n          return cookieStore.getAll()\n        },\n        setAll(cookiesToSet) {\n          try {\n            for (const { name, value, options } of cookiesToSet) {\n              cookieStore.set(name, value, options)\n            }\n          } catch {\n            // setAll يُستدعى أحياناً من Server Component بدلاً من Server Action/Route Handler.\n            // Next.js يمنع الكتابة في هذه الحالة — نتجاهل الخطأ لأن middleware.ts\n            // يتكفّل بتحديث الجلسة في تلك الحالات.\n          }\n        },\n      },\n    }\n  )\n}\n",
  "lib/supabase/admin.ts": "// عميل Supabase بصلاحيات service_role — يتجاوز RLS بالكامل.\n// ⚠️ يُستخدم فقط داخل ملفات Server Actions (\"use server\") — لا تستورده أبداً\n// في أي ملف \"use client\"، وإلا سيتم تضمين المفتاح السري في الحزمة المُرسلة للمتصفح.\n//\n// نحتاجه تحديداً لعملية واحدة: supabase.auth.admin.createUser() و deleteUser()\n// لأن إنشاء/حذف مستخدم Auth بواسطة طرف آخر (المالك) غير مسموح بمفتاح anon.\n\nimport { createClient as createSupabaseClient } from \"@supabase/supabase-js\"\nimport type { Database } from \"./types\"\n\nexport function createAdminClient() {\n  return createSupabaseClient<Database>(\n    process.env.NEXT_PUBLIC_SUPABASE_URL!,\n    process.env.SUPABASE_SERVICE_ROLE_KEY!,\n    {\n      auth: {\n        autoRefreshToken: false,\n        persistSession: false,\n      },\n    }\n  )\n}\n",
  "lib/supabase/types.ts": "// أنواع مطابقة لجداول قاعدة البيانات (يمكن توليدها تلقائياً لاحقاً عبر:\n// npx supabase gen types typescript --project-id <id> > lib/supabase/types.ts)\n\nexport type TenantRow = {\n  id: string\n  user_id: string\n  name: string\n  owner_name: string\n  email: string\n  phone: string\n  plan: \"basic\" | \"professional\" | \"enterprise\"\n  status: \"active\" | \"frozen\" | \"trial\"\n  industry: string\n  currency: string\n  expires_at: string | null\n  created_at: string\n}\n\nexport type UserRoleRow = {\n  user_id: string\n  role: \"owner\" | \"client\"\n  created_at: string\n}\n\nexport interface Database {\n  public: {\n    Tables: {\n      tenants: {\n        Row: TenantRow\n        Insert: Omit<TenantRow, \"id\" | \"created_at\">\n        Update: Partial<Omit<TenantRow, \"id\" | \"user_id\" | \"created_at\">>\n        Relationships: []\n      }\n      user_roles: {\n        Row: UserRoleRow\n        Insert: Omit<UserRoleRow, \"created_at\">\n        Update: Partial<UserRoleRow>\n        Relationships: []\n      }\n    }\n    Views: Record<string, never>\n    Functions: Record<string, never>\n  }\n}\n",
  "proxy.ts": "import { createServerClient } from \"@supabase/ssr\"\nimport { NextResponse, type NextRequest } from \"next/server\"\n\n// يُشغَّل تلقائياً قبل كل طلب — يحافظ على تحديث Cookies الجلسة\n// (Access Token / Refresh Token) حتى لا تنتهي صلاحية الجلسة أثناء استخدام التطبيق.\nexport async function proxy(request: NextRequest) {\n  let response = NextResponse.next({ request })\n\n  const supabase = createServerClient(\n    process.env.NEXT_PUBLIC_SUPABASE_URL!,\n    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,\n    {\n      cookies: {\n        getAll() {\n          return request.cookies.getAll()\n        },\n        setAll(cookiesToSet) {\n          for (const { name, value } of cookiesToSet) {\n            request.cookies.set(name, value)\n          }\n          response = NextResponse.next({ request })\n          for (const { name, value, options } of cookiesToSet) {\n            response.cookies.set(name, value, options)\n          }\n        },\n      },\n    }\n  )\n\n  // هذا الاستدعاء ضروري — يقرأ الجلسة الحالية ويجدّدها إن لزم\n  await supabase.auth.getUser()\n\n  return response\n}\n\nexport const config = {\n  matcher: [\n    \"/((?!_next/static|_next/image|favicon.ico|.*\\\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)\",\n  ],\n}\n",
  "app/actions/tenants.ts": "\"use server\"\n\nimport { createClient } from \"@/lib/supabase/server\"\nimport { createAdminClient } from \"@/lib/supabase/admin\"\nimport type { TenantRow } from \"@/lib/supabase/types\"\n\n/* ------------------------------------------------------------------ */\n/* حارس صلاحية: يتأكد أن المتصل هو المالك قبل تنفيذ أي عملية إدارية    */\n/* ------------------------------------------------------------------ */\nasync function requireOwner() {\n  const supabase = await createClient()\n  const { data: auth } = await supabase.auth.getUser()\n  if (!auth.user) throw new Error(\"غير مصرّح: الرجاء تسجيل الدخول\")\n\n  const { data: roleRow } = await supabase\n    .from(\"user_roles\")\n    .select(\"role\")\n    .eq(\"user_id\", auth.user.id)\n    .single()\n\n  if (roleRow?.role !== \"owner\") throw new Error(\"غير مصرّح: هذا الإجراء للمالك فقط\")\n  return supabase\n}\n\nexport type TenantFormInput = {\n  name: string\n  ownerName: string\n  email: string\n  phone: string\n  tempPassword: string // كلمة مرور أولية تُستخدم مرة واحدة عند الإنشاء فقط\n  plan: TenantRow[\"plan\"]\n  status: TenantRow[\"status\"]\n  industry: string\n  currency: string\n  expiresAt: string\n}\n\n/* ------------------------------------------------------------------ */\n/* قراءة كل العملاء (للمالك فقط — RLS تسمح بذلك تلقائياً)              */\n/* ------------------------------------------------------------------ */\nexport async function getTenantsAction(): Promise<TenantRow[]> {\n  const supabase = await requireOwner()\n  const { data, error } = await supabase\n    .from(\"tenants\")\n    .select(\"*\")\n    .order(\"created_at\", { ascending: false })\n\n  if (error) throw new Error(error.message)\n  return data ?? []\n}\n\n/* ------------------------------------------------------------------ */\n/* إضافة عميل جديد: ينشئ حساب Auth حقيقي + صف بيانات النشاط           */\n/* ------------------------------------------------------------------ */\nexport async function addTenantAction(input: TenantFormInput): Promise<TenantRow> {\n  await requireOwner() // يتأكد أن المستدعي مالك قبل استخدام صلاحيات الأدمن\n\n  if (!input.tempPassword || input.tempPassword.length < 6) {\n    throw new Error(\"كلمة المرور يجب أن تكون 6 أحرف على الأقل\")\n  }\n\n  const admin = createAdminClient()\n\n  // 1. إنشاء مستخدم Auth حقيقي للعميل (بريد مؤكَّد تلقائياً لأن المالك هو من ينشئه)\n  const { data: created, error: createError } = await admin.auth.admin.createUser({\n    email: input.email.trim().toLowerCase(),\n    password: input.tempPassword,\n    email_confirm: true,\n  })\n\n  if (createError || !created.user) {\n    throw new Error(createError?.message ?? \"فشل إنشاء حساب العميل\")\n  }\n\n  // 2. تعيين الدور (client)\n  const { error: roleError } = await admin\n    .from(\"user_roles\")\n    .insert({ user_id: created.user.id, role: \"client\" })\n\n  if (roleError) {\n    await admin.auth.admin.deleteUser(created.user.id) // تراجع (rollback) عند الفشل\n    throw new Error(roleError.message)\n  }\n\n  // 3. إدخال بيانات النشاط التجاري\n  const { data: tenant, error: tenantError } = await admin\n    .from(\"tenants\")\n    .insert({\n      user_id: created.user.id,\n      name: input.name,\n      owner_name: input.ownerName,\n      email: input.email.trim().toLowerCase(),\n      phone: input.phone,\n      plan: input.plan,\n      status: input.status,\n      industry: input.industry,\n      currency: input.currency,\n      expires_at: input.expiresAt || null,\n    })\n    .select()\n    .single()\n\n  if (tenantError || !tenant) {\n    await admin.auth.admin.deleteUser(created.user.id) // rollback كامل\n    throw new Error(tenantError?.message ?? \"فشل حفظ بيانات النشاط التجاري\")\n  }\n\n  return tenant\n}\n\n/* ------------------------------------------------------------------ */\n/* تعديل بيانات عميل (بدون تغيير كلمة المرور من هنا)                   */\n/* ------------------------------------------------------------------ */\nexport async function updateTenantAction(\n  id: string,\n  patch: Partial<Omit<TenantFormInput, \"tempPassword\">>\n): Promise<void> {\n  const supabase = await requireOwner()\n\n  const dbPatch: Partial<Omit<TenantRow, \"id\" | \"user_id\" | \"created_at\">> = {}\n  if (patch.name !== undefined) dbPatch.name = patch.name\n  if (patch.ownerName !== undefined) dbPatch.owner_name = patch.ownerName\n  if (patch.email !== undefined) dbPatch.email = patch.email.trim().toLowerCase()\n  if (patch.phone !== undefined) dbPatch.phone = patch.phone\n  if (patch.plan !== undefined) dbPatch.plan = patch.plan\n  if (patch.status !== undefined) dbPatch.status = patch.status\n  if (patch.industry !== undefined) dbPatch.industry = patch.industry\n  if (patch.currency !== undefined) dbPatch.currency = patch.currency\n  if (patch.expiresAt !== undefined) dbPatch.expires_at = patch.expiresAt || null\n\n  const { error } = await supabase.from(\"tenants\").update(dbPatch).eq(\"id\", id)\n  if (error) throw new Error(error.message)\n}\n\n/* ------------------------------------------------------------------ */\n/* تبديل حالة (نشط/مجمّد)                                              */\n/* ------------------------------------------------------------------ */\nexport async function toggleTenantStatusAction(id: string): Promise<void> {\n  const supabase = await requireOwner()\n\n  const { data: current, error: fetchError } = await supabase\n    .from(\"tenants\")\n    .select(\"status\")\n    .eq(\"id\", id)\n    .single()\n\n  if (fetchError || !current) throw new Error(fetchError?.message ?? \"العميل غير موجود\")\n\n  const nextStatus = current.status === \"active\" ? \"frozen\" : \"active\"\n  const { error } = await supabase.from(\"tenants\").update({ status: nextStatus }).eq(\"id\", id)\n  if (error) throw new Error(error.message)\n}\n\n/* ------------------------------------------------------------------ */\n/* حذف عميل: يحذف حساب Auth بالكامل (يحذف صف tenants تلقائياً عبر     */\n/* on delete cascade في القاعدة)                                       */\n/* ------------------------------------------------------------------ */\nexport async function deleteTenantAction(id: string): Promise<void> {\n  const supabase = await requireOwner()\n\n  const { data: tenant, error: fetchError } = await supabase\n    .from(\"tenants\")\n    .select(\"user_id\")\n    .eq(\"id\", id)\n    .single()\n\n  if (fetchError || !tenant) throw new Error(fetchError?.message ?? \"العميل غير موجود\")\n\n  const admin = createAdminClient()\n  const { error } = await admin.auth.admin.deleteUser(tenant.user_id)\n  if (error) throw new Error(error.message)\n}\n",
  "lib/store.tsx": "\"use client\"\n\nimport {\n  createContext,\n  useCallback,\n  useContext,\n  useMemo,\n  useState,\n  useEffect,\n  type ReactNode,\n} from \"react\"\nimport type {\n  AuthUser,\n  Customer,\n  LedgerEntry,\n  PaymentStatus,\n  Product,\n  Purchase,\n  Sale,\n  StockItem,\n  Tenant,\n  TenantUser,\n  TenantStatus,\n  UnitCode,\n} from \"./types\"\nimport { CURRENCIES } from \"./constants\"\nimport { createClient } from \"./supabase/client\"\nimport type { TenantRow } from \"./supabase/types\"\nimport {\n  addTenantAction,\n  deleteTenantAction,\n  getTenantsAction,\n  toggleTenantStatusAction,\n  updateTenantAction,\n  type TenantFormInput,\n} from \"@/app/actions/tenants\"\n\n/* ------------------------------------------------------------------ */\n/* Helpers                                                            */\n/* ------------------------------------------------------------------ */\n\nconst TODAY = new Date().toISOString().split(\"T\")[0]\n\nconst genId = () =>\n  typeof crypto !== \"undefined\" && \"randomUUID\" in crypto\n    ? crypto.randomUUID()\n    : `id_${Math.random().toString(36).slice(2)}`\n\nexport function remainingBalance(c: Customer): number {\n  return Math.max(0, c.totalCharged - c.amountPaid)\n}\n\nexport function paymentStatus(c: Customer, today: string): PaymentStatus {\n  const remaining = remainingBalance(c)\n  if (remaining <= 0) return \"paid\"\n  if (c.dueDate < today) return \"overdue\"\n  return \"partial\"\n}\n\n// تحويل صف قاعدة البيانات (snake_case) إلى الشكل الذي تتوقعه مكونات الواجهة (camelCase)\nfunction mapTenantRow(row: TenantRow): Tenant {\n  return {\n    id: row.id,\n    name: row.name,\n    ownerName: row.owner_name,\n    email: row.email,\n    phone: row.phone,\n    plan: row.plan,\n    status: row.status,\n    industry: row.industry,\n    currency: row.currency,\n    expiresAt: row.expires_at ?? \"\",\n    createdAt: row.created_at,\n  }\n}\n\n/* ------------------------------------------------------------------ */\n/* Derived: Ledger + Stock                                            */\n/* ------------------------------------------------------------------ */\n\nfunction buildLedger(purchases: Purchase[], sales: Sale[], tenantId: string): LedgerEntry[] {\n  const fromP: LedgerEntry[] = purchases\n    .filter((p) => p.tenantId === tenantId)\n    .map((p) => ({ id: `l_${p.id}`, tenantId, type: \"purchase\" as const, itemName: p.itemName, sku: p.sku, unit: p.unit, quantity: p.quantity, amount: p.quantity * p.unitCost, party: p.supplier, userId: p.userId, date: p.date }))\n  const fromS: LedgerEntry[] = sales\n    .filter((s) => s.tenantId === tenantId)\n    .map((s) => ({ id: `l_${s.id}`, tenantId, type: \"sale\" as const, itemName: s.itemName, sku: s.sku, unit: s.unit, quantity: s.quantity, amount: s.quantity * s.unitPrice, party: s.buyer, userId: s.userId, method: s.method, date: s.date }))\n  return [...fromP, ...fromS].sort((a, b) => (a.date < b.date ? 1 : -1))\n}\n\nfunction buildStock(purchases: Purchase[], sales: Sale[], tenantId: string): StockItem[] {\n  const map = new Map<string, StockItem>()\n  for (const p of purchases.filter((x) => x.tenantId === tenantId)) {\n    const ex = map.get(p.sku) ?? { sku: p.sku, itemName: p.itemName, unit: p.unit, incoming: 0, outgoing: 0, balance: 0, lastCost: 0, lastPrice: 0 }\n    ex.incoming += p.quantity\n    ex.lastCost = p.unitCost\n    ex.itemName = p.itemName\n    ex.unit = p.unit\n    map.set(p.sku, ex)\n  }\n  for (const s of sales.filter((x) => x.tenantId === tenantId)) {\n    const ex = map.get(s.sku) ?? { sku: s.sku, itemName: s.itemName, unit: s.unit, incoming: 0, outgoing: 0, balance: 0, lastCost: 0, lastPrice: 0 }\n    ex.outgoing += s.quantity\n    ex.lastPrice = s.unitPrice\n    ex.itemName = s.itemName\n    ex.unit = s.unit\n    map.set(s.sku, ex)\n  }\n  const items = Array.from(map.values())\n  for (const item of items) item.balance = item.incoming - item.outgoing\n  return items\n}\n\n/* ------------------------------------------------------------------ */\n/* Owner Store Context                                                */\n/* ------------------------------------------------------------------ */\n\ninterface OwnerStoreValue {\n  tenants: Tenant[]\n  loading: boolean\n  error: string | null\n  addTenant: (t: TenantFormInput) => Promise<{ ok: boolean; error?: string }>\n  updateTenant: (id: string, patch: Partial<TenantFormInput>) => Promise<{ ok: boolean; error?: string }>\n  toggleTenantStatus: (id: string) => Promise<void>\n  deleteTenant: (id: string) => Promise<void>\n  refresh: () => Promise<void>\n}\n\nconst OwnerStoreContext = createContext<OwnerStoreValue | null>(null)\n\nexport function useOwnerStore(): OwnerStoreValue {\n  const ctx = useContext(OwnerStoreContext)\n  if (!ctx) throw new Error(\"useOwnerStore must be inside OwnerStoreProvider\")\n  return ctx\n}\n\n/* ------------------------------------------------------------------ */\n/* Auth Context                                                       */\n/* ------------------------------------------------------------------ */\n\ninterface AuthContextValue {\n  authUser: AuthUser | null\n  authLoading: boolean\n  login: (email: string, password: string) => Promise<{ ok: boolean; error?: string }>\n  logout: () => Promise<void>\n}\n\nconst AuthContext = createContext<AuthContextValue | null>(null)\n\nexport function AuthProvider({ children }: { children: ReactNode }) {\n  const [authUser, setAuthUser] = useState<AuthUser | null>(null)\n  const [authLoading, setAuthLoading] = useState(true)\n\n  // يبني كائن AuthUser من جلسة Supabase الحالية عبر قراءة الدور + بيانات التينانت\n  const resolveAuthUser = useCallback(async (): Promise<AuthUser | null> => {\n    const supabase = createClient()\n    const { data: userRes } = await supabase.auth.getUser()\n    const user = userRes.user\n    if (!user) return null\n\n    const { data: roleRow } = await supabase\n      .from(\"user_roles\")\n      .select(\"role\")\n      .eq(\"user_id\", user.id)\n      .single()\n\n    if (!roleRow) return null\n\n    if (roleRow.role === \"owner\") {\n      return {\n        id: user.id,\n        tenantId: null,\n        systemRole: \"owner\",\n        name: \"مشرف المنصة\",\n        email: user.email ?? \"\",\n        username: user.email ?? \"\",\n      }\n    }\n\n    const { data: tenant } = await supabase\n      .from(\"tenants\")\n      .select(\"*\")\n      .eq(\"user_id\", user.id)\n      .single()\n\n    if (!tenant) return null\n\n    return {\n      id: user.id,\n      tenantId: tenant.id,\n      systemRole: \"client\",\n      name: tenant.owner_name || tenant.name,\n      email: tenant.email,\n      username: tenant.owner_name || tenant.email,\n    }\n  }, [])\n\n  // عند التحميل الأول: تحقق من وجود جلسة سابقة (يبقي المستخدم مسجّل دخوله بعد Refresh)\n  // ثم استمع لأي تغيّر لاحق في الجلسة (تسجيل دخول/خروج من أي تبويب)\n  useEffect(() => {\n    const supabase = createClient()\n    let active = true\n\n    resolveAuthUser().then((u) => {\n      if (active) {\n        setAuthUser(u)\n        setAuthLoading(false)\n      }\n    })\n\n    const { data: sub } = supabase.auth.onAuthStateChange(async (_event, session) => {\n      if (!session) {\n        if (active) setAuthUser(null)\n        return\n      }\n      const u = await resolveAuthUser()\n      if (active) setAuthUser(u)\n    })\n\n    return () => {\n      active = false\n      sub.subscription.unsubscribe()\n    }\n  }, [resolveAuthUser])\n\n  const login = useCallback(async (email: string, password: string) => {\n    const supabase = createClient()\n    const cleanEmail = email.trim().toLowerCase()\n    const cleanPassword = password.trim()\n\n    // supabase.auth.signInWithPassword يقارن كلمة المرور بالهاش (bcrypt) المخزّن\n    // فعلياً في auth.users — هذا هو التحقق الحقيقي من قاعدة البيانات.\n    // آمن للاستدعاء من المتصفح لأن الحماية الفعلية هنا هي RLS + هاش كلمة المرور\n    // من طرف Supabase، وليس إخفاء الكود.\n    const { data, error } = await supabase.auth.signInWithPassword({\n      email: cleanEmail,\n      password: cleanPassword,\n    })\n\n    if (error || !data.user) {\n      return { ok: false, error: \"البريد الإلكتروني أو كلمة المرور غير صحيحة\" }\n    }\n\n    const u = await resolveAuthUser()\n    if (!u) {\n      await supabase.auth.signOut()\n      return { ok: false, error: \"الحساب غير مُفعّل بشكل صحيح، الرجاء التواصل مع الدعم\" }\n    }\n\n    // منع دخول العميل المجمَّد حسابه\n    if (u.systemRole === \"client\") {\n      const { data: tenant } = await supabase\n        .from(\"tenants\")\n        .select(\"status\")\n        .eq(\"user_id\", data.user.id)\n        .single()\n\n      if (tenant?.status === \"frozen\") {\n        await supabase.auth.signOut()\n        setAuthUser(null)\n        return { ok: false, error: \"تم تجميد هذا الحساب، الرجاء التواصل مع الإدارة\" }\n      }\n    }\n\n    setAuthUser(u)\n    return { ok: true }\n  }, [resolveAuthUser])\n\n  const logout = useCallback(async () => {\n    const supabase = createClient()\n    await supabase.auth.signOut()\n    setAuthUser(null)\n  }, [])\n\n  return (\n    <AuthContext.Provider value={{ authUser, authLoading, login, logout }}>\n      {children}\n    </AuthContext.Provider>\n  )\n}\n\nexport function useAuth(): AuthContextValue {\n  const ctx = useContext(AuthContext)\n  if (!ctx) throw new Error(\"useAuth must be inside AuthProvider\")\n  return ctx\n}\n\n/* ------------------------------------------------------------------ */\n/* Owner Store Provider Component                                     */\n/* ------------------------------------------------------------------ */\n\nexport function OwnerStoreProvider({ children }: { children: ReactNode }) {\n  const [tenants, setTenants] = useState<Tenant[]>([])\n  const [loading, setLoading] = useState(true)\n  const [error, setError] = useState<string | null>(null)\n\n  const refresh = useCallback(async () => {\n    setLoading(true)\n    setError(null)\n    try {\n      const rows = await getTenantsAction()\n      setTenants(rows.map(mapTenantRow))\n    } catch (e) {\n      setError(e instanceof Error ? e.message : \"فشل تحميل بيانات العملاء\")\n    } finally {\n      setLoading(false)\n    }\n  }, [])\n\n  // تحميل أولي عند دخول المالك للوحة التحكم\n  useEffect(() => {\n    refresh()\n  }, [refresh])\n\n  const addTenant: OwnerStoreValue[\"addTenant\"] = useCallback(async (t) => {\n    try {\n      const row = await addTenantAction(t)\n      setTenants((prev) => [mapTenantRow(row), ...prev])\n      return { ok: true }\n    } catch (e) {\n      const message = e instanceof Error ? e.message : \"فشل إضافة العميل\"\n      return { ok: false, error: message }\n    }\n  }, [])\n\n  const updateTenant: OwnerStoreValue[\"updateTenant\"] = useCallback(async (id, patch) => {\n    try {\n      await updateTenantAction(id, patch)\n      setTenants((prev) => prev.map((t) => (t.id === id ? { ...t, ...patch } as Tenant : t)))\n      return { ok: true }\n    } catch (e) {\n      const message = e instanceof Error ? e.message : \"فشل تعديل بيانات العميل\"\n      return { ok: false, error: message }\n    }\n  }, [])\n\n  const toggleTenantStatus: OwnerStoreValue[\"toggleTenantStatus\"] = useCallback(async (id) => {\n    await toggleTenantStatusAction(id)\n    setTenants((prev) =>\n      prev.map((t) => (t.id === id ? { ...t, status: t.status === \"active\" ? \"frozen\" : \"active\" } : t))\n    )\n  }, [])\n\n  const deleteTenant: OwnerStoreValue[\"deleteTenant\"] = useCallback(async (id) => {\n    await deleteTenantAction(id)\n    setTenants((prev) => prev.filter((t) => t.id !== id))\n  }, [])\n\n  return (\n    <OwnerStoreContext.Provider\n      value={{ tenants, loading, error, addTenant, updateTenant, toggleTenantStatus, deleteTenant, refresh }}\n    >\n      {children}\n    </OwnerStoreContext.Provider>\n  )\n}\n\n/* ------------------------------------------------------------------ */\n/* Client (Tenant) Store Context                                       */\n/* ------------------------------------------------------------------ */\n\nconst numberFmt = new Intl.NumberFormat(\"en-US\", { minimumFractionDigits: 0, maximumFractionDigits: 2 })\n\ninterface ClientStoreValue {\n  today: string\n  tenantId: string\n  tenantUsers: TenantUser[]\n  products: Product[]\n  customers: Customer[]\n  purchases: Purchase[]\n  sales: Sale[]\n  ledger: LedgerEntry[]\n  stock: StockItem[]\n  currentTenantUser: TenantUser | null\n  setCurrentTenantUserId: (id: string) => void\n  currency: (typeof CURRENCIES)[number]\n  setCurrencyCode: (code: string) => void\n  fmt: (amount: number) => string\n  userName: (id: string) => string\n  // Product actions\n  addProduct: (p: Omit<Product, \"id\" | \"createdAt\">) => void\n  updateProduct: (id: string, patch: Partial<Product>) => void\n  deleteProduct: (id: string) => void\n  // Financial actions\n  addTenantUser: (u: Omit<TenantUser, \"id\" | \"createdAt\" | \"lastActive\" | \"status\">) => void\n  updateTenantUser: (id: string, patch: Partial<TenantUser>) => void\n  toggleTenantUserStatus: (id: string) => void\n  deleteTenantUser: (id: string) => void\n  addPurchase: (p: Omit<Purchase, \"id\" | \"tenantId\">) => void\n  addSale: (s: Omit<Sale, \"id\" | \"tenantId\">) => void\n  recordPayment: (customerId: string, amount: number) => void\n  addCustomer: (c: Omit<Customer, \"id\" | \"updatedAt\" | \"tenantId\">) => void\n}\n\nconst ClientStoreContext = createContext<ClientStoreValue | null>(null)\n\nexport function ClientStoreProvider({\n  tenantId, initialCurrency, children,\n}: { tenantId: string; initialCurrency?: string; children: ReactNode }) {\n  const [tenantUsers, setTenantUsers]   = useState<TenantUser[]>([])\n  const [products, setProducts]         = useState<Product[]>([])\n  const [customers, setCustomers]       = useState<Customer[]>([])\n  const [purchases, setPurchases]       = useState<Purchase[]>([])\n  const [sales, setSales]               = useState<Sale[]>([])\n  const [currentTenantUserId, setCurrentTenantUserId] = useState<string>(\"\")\n  const [currencyCode, setCurrencyCode] = useState<string>(initialCurrency ?? \"ILS\")\n\n  const currency = useMemo(() => CURRENCIES.find((c) => c.code === currencyCode) ?? CURRENCIES[0], [currencyCode])\n  const fmt = useCallback((amount: number) => `${numberFmt.format(amount)} ${currency.symbol}`, [currency])\n  const currentTenantUser = useMemo(() => tenantUsers.find((u) => u.id === currentTenantUserId) ?? tenantUsers[0] ?? null, [tenantUsers, currentTenantUserId])\n  const ledger = useMemo(() => buildLedger(purchases, sales, tenantId), [purchases, sales, tenantId])\n  const stock  = useMemo(() => buildStock(purchases, sales, tenantId),  [purchases, sales, tenantId])\n  const userName = useCallback((id: string) => tenantUsers.find((u) => u.id === id)?.name ?? \"غير معروف\", [tenantUsers])\n\n  // ── Product CRUD ────────────────────────────────────────────────\n  const addProduct = useCallback((p: Omit<Product, \"id\" | \"createdAt\">) => {\n    setProducts((prev) => [{ ...p, id: genId(), createdAt: TODAY }, ...prev])\n  }, [])\n  const updateProduct = useCallback((id: string, patch: Partial<Product>) => {\n    setProducts((prev) => prev.map((p) => (p.id === id ? { ...p, ...patch } : p)))\n  }, [])\n  const deleteProduct = useCallback((id: string) => {\n    setProducts((prev) => prev.filter((p) => p.id !== id))\n  }, [])\n\n  // ── User CRUD ────────────────────────────────────────────────────\n  const addTenantUser = useCallback((u: Omit<TenantUser, \"id\" | \"createdAt\" | \"lastActive\" | \"status\">) => {\n    setTenantUsers((prev) => [{ ...u, id: genId(), status: \"active\", createdAt: TODAY, lastActive: TODAY }, ...prev])\n  }, [])\n  const updateTenantUser = useCallback((id: string, patch: Partial<TenantUser>) => {\n    setTenantUsers((prev) => prev.map((u) => (u.id === id ? { ...u, ...patch } : u)))\n  }, [])\n  const toggleTenantUserStatus = useCallback((id: string) => {\n    setTenantUsers((prev) => prev.map((u) => u.id === id ? { ...u, status: u.status === \"active\" ? \"frozen\" : \"active\" } : u))\n  }, [])\n  const deleteTenantUser = useCallback((id: string) => {\n    setTenantUsers((prev) => prev.filter((u) => u.id !== id))\n  }, [])\n\n  // ── Purchase / Sale ──────────────────────────────────────────────\n  const addPurchase = useCallback((p: Omit<Purchase, \"id\" | \"tenantId\">) => {\n    setPurchases((prev) => [{ ...p, id: genId(), tenantId }, ...prev])\n    setProducts((prev) => prev.map((prod) => prod.sku === p.sku ? { ...prod, lastCost: p.unitCost } : prod))\n  }, [tenantId])\n\n  const addSale = useCallback((s: Omit<Sale, \"id\" | \"tenantId\">) => {\n    const newSale: Sale = { ...s, id: genId(), tenantId }\n    setSales((prev) => [newSale, ...prev])\n    setProducts((prev) => prev.map((prod) => prod.sku === s.sku ? { ...prod, lastPrice: s.unitPrice } : prod))\n    if (s.method === \"debt\") {\n      const total = s.quantity * s.unitPrice\n      setCustomers((prev) => {\n        const match = prev.find((c) => c.name.toLowerCase() === s.buyer.toLowerCase())\n        if (match) {\n          return prev.map((c) => c.id === match.id ? {\n            ...c,\n            itemsDetail: c.itemsDetail ? `${c.itemsDetail}، ${s.quantity} ${s.unit} ${s.itemName}` : `${s.quantity} ${s.unit} ${s.itemName}`,\n            totalCharged: c.totalCharged + total,\n            updatedAt: TODAY,\n          } : c)\n        }\n        return [{ id: genId(), tenantId, name: s.buyer, phone: \"—\", accountId: `ACC-${Math.floor(1000 + Math.random() * 9000)}`, itemsDetail: `${s.quantity} ${s.unit} ${s.itemName}`, totalCharged: total, amountPaid: 0, dueDate: TODAY, updatedAt: TODAY }, ...prev]\n      })\n    }\n  }, [tenantId])\n\n  const recordPayment = useCallback((customerId: string, amount: number) => {\n    setCustomers((prev) => prev.map((c) => c.id === customerId ? { ...c, amountPaid: Math.min(c.totalCharged, c.amountPaid + amount), updatedAt: TODAY } : c))\n  }, [])\n\n  const addCustomer = useCallback((c: Omit<Customer, \"id\" | \"updatedAt\" | \"tenantId\">) => {\n    setCustomers((prev) => [{ ...c, id: genId(), tenantId, updatedAt: TODAY }, ...prev])\n  }, [tenantId])\n\n  const value: ClientStoreValue = {\n    today: TODAY, tenantId, tenantUsers, products, customers, purchases, sales, ledger, stock,\n    currentTenantUser, setCurrentTenantUserId, currency, setCurrencyCode, fmt, userName,\n    addProduct, updateProduct, deleteProduct,\n    addTenantUser, updateTenantUser, toggleTenantUserStatus, deleteTenantUser,\n    addPurchase, addSale, recordPayment, addCustomer,\n  }\n  return <ClientStoreContext.Provider value={value}>{children}</ClientStoreContext.Provider>\n}\n\nexport function useClientStore(): ClientStoreValue {\n  const ctx = useContext(ClientStoreContext)\n  if (!ctx) throw new Error(\"useClientStore must be inside ClientStoreProvider\")\n  return ctx\n}",
  "components/auth/login-page.tsx": "\"use client\"\n\nimport { useState } from \"react\"\nimport { useAuth } from \"@/lib/store\"\nimport { Eye, EyeOff, Lock, Mail, Calculator } from \"lucide-react\"\nimport { cn } from \"@/lib/utils\"\n\nexport function LoginPage() {\n  const { login } = useAuth()\n  const [email, setEmail] = useState(\"\")\n  const [password, setPassword] = useState(\"\")\n  const [showPass, setShowPass] = useState(false)\n  const [error, setError] = useState(\"\")\n  const [loading, setLoading] = useState(false)\n\n  async function handleSubmit(e: React.FormEvent) {\n    e.preventDefault()\n    setError(\"\")\n    setLoading(true)\n    const result = await login(email.trim(), password)\n    if (!result.ok) setError(result.error ?? \"تعذّر تسجيل الدخول\")\n    setLoading(false)\n  }\n\n  return (\n    <div className=\"min-h-screen bg-gradient-to-br from-slate-900 via-blue-950 to-slate-900 flex items-center justify-center p-4\">\n      {/* Background pattern */}\n      <div className=\"pointer-events-none fixed inset-0 opacity-5\"\n        style={{ backgroundImage: \"radial-gradient(circle at 1px 1px, white 1px, transparent 0)\", backgroundSize: \"40px 40px\" }}\n      />\n\n      <div className=\"w-full max-w-md relative\">\n        {/* Logo + Brand */}\n        <div className=\"text-center mb-8\">\n          <div className=\"inline-flex items-center justify-center w-16 h-16 rounded-2xl bg-blue-600 shadow-2xl shadow-blue-600/40 mb-4\">\n            <Calculator className=\"size-8 text-white\" />\n          </div>\n          <h1 className=\"text-4xl font-extrabold text-white tracking-tight\">مُحاسِب</h1>\n          <p className=\"text-blue-300/80 text-sm mt-1\">نظام المحاسبة السحابي ونقاط البيع</p>\n        </div>\n\n        {/* Login Card */}\n        <div className=\"bg-white/5 border border-white/10 backdrop-blur-xl rounded-3xl p-8 shadow-2xl\">\n          <h2 className=\"text-xl font-bold text-white mb-6\">تسجيل الدخول</h2>\n\n          <form onSubmit={handleSubmit} className=\"space-y-4\">\n            <div>\n              <label className=\"block text-sm font-medium text-blue-200 mb-1.5\">\n                البريد الإلكتروني\n              </label>\n              <div className=\"relative\">\n                <Mail className=\"pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 size-4 text-blue-400\" />\n                <input\n                  type=\"email\"\n                  value={email}\n                  onChange={(e) => setEmail(e.target.value)}\n                  placeholder=\"name@example.com\"\n                  className=\"w-full h-12 rounded-xl bg-white/10 border border-white/10 pr-10 pl-4 text-white placeholder:text-white/30 focus:outline-none focus:border-blue-400 focus:ring-2 focus:ring-blue-400/30 transition text-sm\"\n                  autoComplete=\"email\"\n                  dir=\"ltr\"\n                />\n              </div>\n            </div>\n\n            <div>\n              <label className=\"block text-sm font-medium text-blue-200 mb-1.5\">كلمة المرور</label>\n              <div className=\"relative\">\n                <Lock className=\"pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 size-4 text-blue-400\" />\n                <input\n                  type={showPass ? \"text\" : \"password\"}\n                  value={password}\n                  onChange={(e) => setPassword(e.target.value)}\n                  placeholder=\"أدخل كلمة المرور\"\n                  className=\"w-full h-12 rounded-xl bg-white/10 border border-white/10 pr-10 pl-10 text-white placeholder:text-white/30 focus:outline-none focus:border-blue-400 focus:ring-2 focus:ring-blue-400/30 transition text-sm\"\n                  autoComplete=\"current-password\"\n                />\n                <button\n                  type=\"button\"\n                  onClick={() => setShowPass((v) => !v)}\n                  className=\"absolute left-3 top-1/2 -translate-y-1/2 text-blue-400 hover:text-blue-200 transition\"\n                >\n                  {showPass ? <EyeOff className=\"size-4\" /> : <Eye className=\"size-4\" />}\n                </button>\n              </div>\n            </div>\n\n            {error && (\n              <div className=\"rounded-xl bg-red-500/20 border border-red-400/30 px-4 py-3 text-sm text-red-300\">\n                {error}\n              </div>\n            )}\n\n            <button\n              type=\"submit\"\n              disabled={loading || !email}\n              className={cn(\n                \"w-full h-12 rounded-xl font-bold text-sm transition-all\",\n                loading || !email\n                  ? \"bg-blue-600/40 text-blue-300/50 cursor-not-allowed\"\n                  : \"bg-blue-600 hover:bg-blue-500 text-white shadow-lg shadow-blue-600/40 active:scale-[0.98]\"\n              )}\n            >\n              {loading ? \"جارٍ تسجيل الدخول...\" : \"دخول\"}\n            </button>\n          </form>\n        </div>\n\n        <p className=\"text-center text-blue-400/40 text-xs mt-6\">\n          مُحاسِب © 2026 — جميع الحقوق محفوظة\n        </p>\n      </div>\n    </div>\n  )\n}",
};

let created = 0;
let skipped = 0;

for (const [relPath, content] of Object.entries(files)) {
  const fullPath = path.join(process.cwd(), relPath);
  const dir = path.dirname(fullPath);

  if (fs.existsSync(fullPath) && !FORCE) {
    console.log("⏭️  تخطّي (موجود مسبقاً، استخدم --force للاستبدال): " + relPath);
    skipped++;
    continue;
  }

  fs.mkdirSync(dir, { recursive: true });
  fs.writeFileSync(fullPath, content, "utf8");
  console.log("✅ تم إنشاء: " + relPath);
  created++;
}

console.log("");
console.log("تم إنشاء " + created + " ملف، وتخطّي " + skipped + " ملف.");
console.log("");
console.log("الخطوات التالية:");
console.log("  1) npm install @supabase/supabase-js @supabase/ssr");
console.log("  2) انسخ .env.local.example إلى .env.local واملأ القيم من Supabase Dashboard");
console.log("  3) نفّذ supabase/schema.sql في Supabase SQL Editor");
console.log("  4) أنشئ حساب Owner يدوياً من Authentication → Users، ثم أضِف صفه في user_roles (راجع تعليمات نهاية schema.sql)");
